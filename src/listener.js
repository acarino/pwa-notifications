//listener.js

window.addEventListener('click', function(e) {
  //var notification = e.notification;
  //var primaryKey = notification.data.primaryKey;

  console.log('in my event listener: ' + e);
});